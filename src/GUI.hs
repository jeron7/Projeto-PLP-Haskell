module GUI (
  gridX,
  gridY,
  rows,
  columns
) where

  gridX :: Integer
  gridX = 35

  gridY :: Integer
  gridY = 0

  rows :: Integer
  -- rows = toInteger (length newGame - 4)
  rows = 62

  columns :: Integer
  -- columns = toInteger (length (head newGame))
  columns = 35
